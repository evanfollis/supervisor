---
from: synthesis-translator
to: general
date: 2026-06-11T03:35:04Z
priority: highest
task_id: synthesis-p2
source_synthesis: /opt/workspace/runtime/.meta/cross-cutting-2026-06-11T03-27-59Z.md
source_proposal: Proposal 2 — P2
---

# P2 — Add `events/` to autocommit Tier-A scope (33rd cycle)

## Proposal summary

Add `events/` directory to the Tier-A governance auto-commit scope in `supervisor-autocommit.sh`. This is a one-line scope change (two locations) that unblocks the entire governance cascade and closes a 33-cycle carry-forward. This is the sole tick blocker per the synthesis.

## Full proposal body (from synthesis)

**Type:** One-line scope change — `supervisor/scripts/lib/supervisor-autocommit.sh`

Identical to C90 §Proposed #2, C89 §Proposed #1, and every synthesis since C60. **33rd consecutive cycle as an open recommendation.** Still the sole tick blocker. The fix:

```bash
# Lines 55-56: add events/ to the dirty check
TIER_A_DIRTY=$(git -C "$SUP" status --porcelain \
  -- friction/ handoffs/ system/ ideas/ decisions/ events/ 2>/dev/null || true)

# Lines 61-62: add events/ to the staging
git -C "$SUP" add \
  friction/ handoffs/ system/ ideas/ decisions/ events/ \
  2>/dev/null || true
```

## Rationale

From synthesis Pattern 3: "Unpushed commits accelerating — auto-commit gate creates ~8 commits/day with no push path." The `events/` directory contains telemetry and operational records that should be part of Tier-A governance, but they are currently excluded from the autocommit scope. This gap means:

1. Event files remain dirty in supervisor repo
2. The autocommit gate skips them
3. Unpushed commits accumulate
4. Atlas has filed its own URGENT for this after 3 cycles

Adding `events/` to the autocommit scope is the sole action blocking the governance loop from moving forward. This has been the #1 recommendation for 33 consecutive synthesis cycles. The fix is zero-risk, zero-credentials, one-line changes in two places.

## Verification before action (required)

- Verify that `supervisor/scripts/lib/supervisor-autocommit.sh` exists and contains the lines referenced (55-56 for dirty check, 61-62 for staging).
- Confirm that the current dirty check does NOT include `events/` (scan line 55-56).
- Confirm that the current staging does NOT include `events/` (scan line 61-62).
- If both `events/` entries are already present in the file, write a completion report: "already landed in supervisor-autocommit.sh commit <SHA>" and close.

## Acceptance criteria

- Line 55-56 (TIER_A_DIRTY dirty-check) now includes `events/` alongside `friction/ handoffs/ system/ ideas/ decisions/`.
- Line 61-62 (git add staging) now includes `events/` alongside `friction/ handoffs/ system/ ideas/ decisions/`.
- The two-line amendment is committed with a message explaining this closes a 33-cycle carry-forward and unblocks the governance tick.
- The commit message should note that `events/` is Tier-A governance telemetry and belongs in autocommit scope (e.g. "autocommit: include events/ in Tier-A governance scope (C91 Proposal 2, 33-cycle carry)").
- Completion report at `runtime/.handoff/general-supervisor-synthesis-p2-complete-<iso>.md` confirming the amendment is landed.

## Escalation

**URGENT if:**
- The current state of the file shows `events/` is already included. This would indicate the proposal is based on stale state and has already landed via another path. Write "obsolete — already landed" completion report and close.
- The file location is different from the path provided (would indicate a move/rename since the synthesis ran).

Otherwise, this is a mechanical scope addition with zero functional risk. Land it and report completion.

## Critical context for executor

From the synthesis "Standing recommendations" table: **This is the SOLE TICK BLOCKER.** The synthesis explicitly states (Pattern 5): "The gap between diagnosis and execution has been growing for 17+ days (since C56, the last dispatched synthesis)." This proposal, in isolation, unblocks the entire governance dispatch queue. The synthesis marks it "HIGHEST" priority — treat it accordingly.

