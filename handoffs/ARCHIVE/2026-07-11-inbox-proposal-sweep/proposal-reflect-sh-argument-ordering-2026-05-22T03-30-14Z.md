---
from: synthesis-translator
to: general
date: 2026-05-22T03:30:14Z
priority: high
task_id: synthesis-reflect-sh-fix
source_synthesis: /opt/workspace/runtime/.meta/cross-cutting-2026-05-22T03-23-17Z.md
source_proposal: Proposal 1 — reflect.sh argument ordering
---

# reflect.sh argument ordering — route to supervisor session

**Type:** Shared primitive fix.

**Action:** Fix `scripts/lib/reflect.sh:193` under existing charter authority (AGENT.md:508-511). Move `-- CURRENT_STATE.md` to after the three `-m` flags:

```bash
# Before (broken):
if git -C "$PROJECT_DIR" commit --only -- CURRENT_STATE.md \
     -m "reflect: auto-update CURRENT_STATE.md ${ISO_NOW}" \
     ...

# After (fixed):
if git -C "$PROJECT_DIR" commit --only \
     -m "reflect: auto-update CURRENT_STATE.md ${ISO_NOW}" \
     -m "Generated by scripts/lib/reflect.sh after the ${PROJECT} reflection session." \
     -m "session_id=reflect-${PROJECT}-${ISO_NOW}" \
     -- CURRENT_STATE.md \
```

**Blast radius:** All reflected projects (automatic). Argument reorder only — no logic change.

**Status:** No longer a principal question. Charter authorizes it. This supervisor session should execute.

## Verification before action (required)

- Run `git log --oneline -20 -- supervisor/scripts/lib/reflect.sh` on the supervisor repo. Check if this fix has already landed.
- Read `supervisor/scripts/lib/reflect.sh:190-200`. Verify the `--` still appears before the `-m` flags (indicating the bug is still present).
- If either is not true, write a completion report stating the fix has already landed and close.

## Acceptance criteria

- The `--` option terminator is moved to after all `-m` flags in the `git commit --only` command.
- Change committed with a clear message explaining this resolves the CURRENT_STATE.md autocommit bug that has affected all reflected projects.
- Completion report at `runtime/.handoff/general-supervisor-reflect-sh-fix-complete-<iso>.md` pointing back to this handoff and the source synthesis.

## Escalation

URGENT if:
- Primary verification reveals the fix has already been applied by another path since the synthesis ran. Write a completion report saying "already landed at commit <SHA>" and close.
- The fix breaks any other git operations or violates a constraint the translator missed.
